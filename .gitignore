# ── Secrets & Environment ──────────────────────────────────────────
.env
.env.*
!.env.example

# ── Python ────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
*.egg
dist/
build/
*.whl

# ── Virtual Environment ───────────────────────────────────────────
.venv/
venv/
env/

# ── IDE ───────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~

# ── Claude Code ───────────────────────────────────────────────────
.claude/

# ── OS files ──────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Reports (generated, large HTML files) ─────────────────────────
reports/*.html

# ── Analysis cache (contains price data, not secrets) ─────────────
data/analyses/

# ── Portfolio Baseline (private — never upload) ───────────────────
data/portfolio_baseline.db
data/portfolio_baseline.db-*
data/portfolio_baseline.json

# ── Per-user database files (private — never upload) ──────────────
data/*.db
data/*.db-*
data/*.sqlite
data/*.sqlite-*
data/comparison_cache/
data/favorites.json
